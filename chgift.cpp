#include <bits/stdc++.h>

using namespace std;
typedef long long LL;
typedef unsigned long long ULL;

int main()
{
// Uncomment to use TestCases
	int T, n;
	long long m[11], p[11], m[11], a[10];
	scanf("%d", &T);
	while(T--)
	{
		scanf("%d", &n);
		for(int i=0; i<n; i++)
			scanf("%d", a+i);
		m[0] = p[0] = m[0] = a[0];
		for(int i=1; i<n; i++)
		{
			m[0] = min(
	}
	return 0;
}

